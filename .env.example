# Copy to .env for a local (non-Docker) run and point at a Postgres you control.
# When running via docker compose these are set in docker-compose.yml instead.
DATABASE_URL=postgres://listings:listings@localhost:5432/listings?sslmode=disable
PORT=8080
